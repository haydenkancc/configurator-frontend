import { Storage } from '..';

export interface DriveBayDto {
    formFactor: Storage.FormFactorDto;
    driveCount: number;
}

export interface DriveBayDbo {
    formFactorID: number;
    driveCount: number;
}