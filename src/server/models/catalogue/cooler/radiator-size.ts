export interface RadiatorSizeListItem {
    id: number;
    length: number;
}

export interface RadiatorSizeDto {
    id: number;
    length: number;
}

export interface RadiatorSizeDbo {
    length: number;
}